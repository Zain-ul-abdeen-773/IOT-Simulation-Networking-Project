% ============================================================
%  CHAPTER 4 — MACHINE LEARNING PIPELINE
% ============================================================
\chapter{Machine Learning Pipeline}

\section{Overview}

The Aegis-IoT learning pipeline is designed for low-friction experimentation and deployment inside a JVM-based simulator. It follows three phases:
\begin{enumerate}
    \item \textbf{Offline training:} models are trained from cleaned telemetry using standard ML tooling (scikit-learn) \cite{sklearn}.
    \item \textbf{Model export:} classical models are exported to native Java using model-to-code generation \cite{m2cgen}.
    \item \textbf{In-simulation inference:} exported predictors execute inline during packet processing.
\end{enumerate}

Figure~\ref{fig:ml_pipeline} illustrates this three-phase workflow.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    phase/.style={rectangle, rounded corners=6pt, draw=#1!60, fill=#1!8,
        minimum width=4.0cm, minimum height=1.6cm,
        font=\small\bfseries, align=center, text=#1!80!black},
    detail/.style={rectangle, rounded corners=4pt, draw=black!25, fill=white,
        minimum width=3.6cm, minimum height=0.7cm,
        font=\scriptsize, align=center},
    arr/.style={-{Stealth[length=3mm]}, thick, black!60},
]
% Phase boxes
\node[phase=blue] (P1) {Phase 1\\Offline Training};
\node[phase=violet, right=3.0cm of P1] (P2) {Phase 2\\Model Export};
\node[phase=teal, right=3.0cm of P2] (P3) {Phase 3\\In-Simulation Inference};

% Detail boxes below
\node[detail, below=0.6cm of P1] (D1a) {scikit-learn (Python)};
\node[detail, below=0.3cm of D1a] (D1b) {RT-IoT2022 dataset};
\node[detail, below=0.6cm of P2] (D2a) {m2cgen transpiler};
\node[detail, below=0.3cm of D2a] (D2b) {\texttt{.py} $\to$ \texttt{.java}};
\node[detail, below=0.6cm of P3] (D3a) {AnyLogic JVM runtime};
\node[detail, below=0.3cm of D3a] (D3b) {Zero-dependency inference};

% Arrows
\draw[arr] (P1) -- node[above, font=\scriptsize]{.pkl models} (P2);
\draw[arr] (P2) -- node[above, font=\scriptsize]{.java classes} (P3);

% Output labels
\node[below=0.3cm of D1b, font=\tiny\itshape, text=black!50] {Random Forest, SVM, BiLSTM};
\node[below=0.3cm of D2b, font=\tiny\itshape, text=black!50] {Pure Java source code};
\node[below=0.3cm of D3b, font=\tiny\itshape, text=black!50] {Sub-ms per-packet scoring};
\end{tikzpicture}%
}
\caption{Three-phase ML pipeline: offline training in Python, model export to Java via m2cgen, and zero-dependency inference inside the AnyLogic JVM.}
\label{fig:ml_pipeline}
\end{figure}

\section{Data and Features}

Experiments are based on the RT-IoT2022 dataset \cite{rt-iot}, cleaned into 4,132 packet-flow records. Features are derived from packet size, inter-arrival timing, and observed flow duration. Three feature views are used:
\begin{itemize}
    \item \textbf{Latency prediction:} packetSize and interArrival.
    \item \textbf{Anomaly detection:} packetSize, interArrival, and flowDuration.
    \item \textbf{Forecasting:} sliding windows over recent flowDuration values.
\end{itemize}

\section{Models Deployed}

Table~\ref{tab:model_summary} summarizes the models used in the platform and how they are executed.

\begin{table}[H]
\centering
\caption{Model Summary and Runtime Integration}
\label{tab:model_summary}
\begin{tabularx}{\textwidth}{@{}lXl@{}}
\toprule
\textbf{Capability} & \textbf{Model and Inputs} & \textbf{Output} \\
\midrule
Latency prediction & Random Forest regressor (\texttt{OfflineAiPredictor}) using (packetSize, interArrival) \cite{rf} & Estimated flow duration \\
Anomaly scoring & One-Class SVM (\texttt{AnomalyModel}) using (packetSize, interArrival, flowDuration) \cite{ocsvm} & Novelty score / flag \\
Short-horizon forecasting & BiLSTM forecaster over recent flowDuration windows, predicting t+10 & Predicted future latency \\
\bottomrule
\end{tabularx}
\end{table}

\section{Evaluation Metric}

For regression tasks, performance is reported using Mean Absolute Error (MAE):
\begin{equation}
\text{MAE} = \frac{1}{n}\sum_{i=1}^{n}\left|y_i - \hat{y}_i\right|
\end{equation}

\section{Deployment Rationale}

Exporting models into Java source enables in-process inference inside AnyLogic without external services, serialization steps, or cross-language RPC overhead. This keeps experiments reproducible and reduces engineering complexity for the simulation runtime.

This in-process approach minimizes inference latency for decision-making during packet processing, simplifies deployment by removing external serving infrastructure, and accelerates iteration for model updates. Tight coupling between simulator state and predictor inputs improves fidelity of offline-to-online evaluation, allowing more realistic validation of control strategies. Finally, shipping lightweight exported models reduces the operational footprint for demonstration and teaching scenarios.
