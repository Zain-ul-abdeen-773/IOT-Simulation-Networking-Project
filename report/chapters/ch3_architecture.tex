% ============================================================
%  CHAPTER 3 — SYSTEM ARCHITECTURE
% ============================================================
\chapter{System Architecture}

\section{High-Level Architecture}

The Aegis-IoT platform is organized into a small set of layers that connect simulation, intelligence, and observability. Figure~\ref{fig:arch} summarizes the main components and data flow.

\begin{figure}[p]
\centering
% Mermaid-equivalent architecture diagram (TikZ)
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    node distance=1.6cm and 2.5cm,
    layer/.style={rectangle, rounded corners=6pt, draw=black!30, fill=#1!10, text=black, minimum width=4.2cm, minimum height=1.1cm, font=\small\bfseries},
    comp/.style={rectangle, rounded corners=4pt, draw=black!40, fill=white, minimum width=2.6cm, minimum height=0.8cm, font=\footnotesize},
    core/.style={rectangle, rounded corners=6pt, draw=accentPurple!70, fill=accentPurple!8, minimum width=5cm, minimum height=1.2cm, font=\small\bfseries},
    vis/.style={rectangle, rounded corners=6pt, draw=accentBlue!70, fill=accentBlue!8, minimum width=5cm, minimum height=1.2cm, font=\small\bfseries},
    arrow/.style={-{Stealth[length=3mm]}, thick, black!70},
    darrow/.style={-{Stealth[length=3mm]}, thick, dashed, black!50},
]

    % Data Acquisition Layer
    \node[layer=blue] (DA) {Data Acquisition Layer};
    \node[comp, below left=1.2cm and 1.0cm of DA] (SDA) {SensorDevice Agents};
    \node[comp, below=1.2cm of DA] (MQTT) {MQTT Packets};
    \node[comp, below right=1.2cm and 1.0cm of DA] (GN) {Gateway Nodes};

    % Intelligence Core
    \node[core, below=4cm of DA] (IC) {Intelligence Core (JVM)};
    % spread the children wider and increase vertical gap to avoid overlap
    \node[comp, below=3.0cm of IC, xshift=-8.0cm] (LP) {Latency Predictor};
    \node[comp, below=3.0cm of IC, xshift=-4.0cm] (AD) {Anomaly Detector};
    \node[comp, below=3.0cm of IC, xshift=0cm]    (GAN) {GAN DDoS Synthesis};
    \node[comp, below=3.0cm of IC, xshift=4.0cm]  (TSF) {Time-Series Forecaster};
    \node[comp, below=3.0cm of IC, xshift=8.0cm]  (RLP) {RL Policy};

    % Visualization Layer
    \node[vis, below=6.5cm of IC] (VL) {Visualization Layer};
    % place five NOC nodes with larger horizontal spacing
    \node[comp, below=3.6cm of VL, xshift=-11.0cm] (NOC1) {NOC: Latency Dashboard\\\small(LatencyDash)};
    \node[comp, below=3.6cm of VL, xshift=-5.5cm]  (NOC2) {NOC: Security Radar\\\small(SecurityDash)};
    \node[comp, below=3.6cm of VL, xshift=0cm]     (NOC3) {NOC: Telemetry Matrix\\\small(TelemetryDash)};
    \node[comp, below=3.6cm of VL, xshift=5.5cm]   (NOC4) {NOC: Energy Digital Twin\\\small(EnergyDash)};
    \node[comp, below=3.6cm of VL, xshift=11.0cm]  (NOC5) {NOC: Operations \\& Event Log\\\small(ReportDash)};
    % Web dashboard (React/Vite)
    \node[comp, right=2.6cm of VL] (WEB) {Web Dashboard\\\small(React/Vite)};

    % Connections
    \draw[arrow] (SDA) -- (MQTT);
    \draw[arrow] (MQTT) -- (GN);
    \draw[arrow] (GN) -- (IC);
    \draw[arrow] (IC) -- (LP);
    \draw[arrow] (IC) -- (AD);
    \draw[arrow] (IC) -- (GAN);
    \draw[arrow] (IC) -- (TSF);
    \draw[arrow] (IC) -- (RLP);
    \draw[darrow] (RLP.south) .. controls +(-6mm,-8mm) and +(-8mm,6mm) .. (GN.west);
    \draw[arrow] (IC) -- (VL);
    \draw[arrow] (VL) -- (NOC1);
    \draw[arrow] (VL) -- (NOC2);
    \draw[arrow] (VL) -- (NOC3);
    \draw[arrow] (VL) -- (NOC4);
    \draw[arrow] (VL) -- (NOC5);
    \draw[arrow] (IC) -- (WEB);

\end{tikzpicture}%
}
\vspace{0.8em}
\caption{Aegis-IoT system architecture from packet generation to intelligence and visualization.}
\label{fig:arch}
\end{figure}

\section{Simulation Model}

The simulation is implemented in AnyLogic \cite{anylogic} using an agent-based discrete-event approach. Table~\ref{tab:agents} lists the principal agent types.

\begin{table}[H]
\centering
\caption{AnyLogic Agent Types}
\label{tab:agents}
\begin{tabularx}{\textwidth}{@{}lXl@{}}
\toprule
\textbf{Agent} & \textbf{Role} & \textbf{Count} \\
\midrule
SensorDevice & Generates telemetry packets using dataset-derived distributions & Multiple \\
MQTTPacket & Carries network metrics through the simulated path & Dynamic \\
Main & Orchestrates execution and hosts the intelligence pipeline & 1 \\
\bottomrule
\end{tabularx}
\end{table}

Each packet carries a small set of features used for analytics and visualization (Table~\ref{tab:mqttfields}).

\begin{table}[H]
\centering
\caption{Packet-Level Network Metrics}
\label{tab:mqttfields}
\begin{tabularx}{\textwidth}{@{}llX@{}}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Meaning} \\
\midrule
packet\_size & double & Payload size (bytes) \\
inter\_arrival & double & Time between arrivals (\textmu s) \\
flow\_duration & double & End-to-end flow duration (ms) \\
\bottomrule
\end{tabularx}
\end{table}

\section{Processing Pipeline}

When packets reach the cloud sink, the simulation executes a fixed sequence:
\begin{enumerate}
    \item optional stress-traffic injection to emulate attack-like outliers,
    \item congestion-aware control decision via the RL policy,
    \item feature preparation and model inference (prediction, anomaly scoring, forecasting), and
    \item broadcast of updated telemetry to the visualization layers.
\end{enumerate}

\section{Dataset}

Experiments use RT-IoT2022 MQTT traffic \cite{rt-iot}. After preprocessing, a cleaned dataset of 4,132 flow records is used to calibrate distributions and train models. Table~\ref{tab:dataset} reports summary statistics for the core fields.

\begin{table}[H]
\centering
\caption{Dataset Statistics (clean\_mqtt\_traffic.csv)}
\label{tab:dataset}
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Feature} & \textbf{Mean} & \textbf{Std Dev} & \textbf{Min} & \textbf{Max} \\
\midrule
packetSize & 7.71 & 2.14 & 3.43 & 15.00 \\
interArrival & 2,462,430 & 845,212 & 1,024 & 4,921,000 \\
flowDuration & 32.01 & 10.98 & 0.13 & 64.00 \\
\bottomrule
\end{tabular}
\end{table}
